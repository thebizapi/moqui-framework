<addons default-repository="github-ssh">
    <!-- Where to get runtime directory if not present -->
    <runtime name="moqui-runtime" group="thebizapi" branch="thebizapi"/>

    <!-- Mantle Business Artifact and Integration Components -->
    <component name="mantle-udm" group="moqui" branch="master"/>
    <component name="mantle-usl" group="moqui" branch="master"/>

    <!-- Moqui Applications -->
    <component name="SimpleScreens" group="moqui" branch="master"/>
    <component name="HiveMind" group="moqui" branch="master"/>
    <component name="PopCommerce" group="moqui" branch="master"/>
    <component name="MarbleERP" group="moqui" branch="master"/>

    <!-- Moqui Tool Components -->
<!--    <component name="moqui-hazelcast" group="moqui" branch="master"/>-->

    <!-- Moqui Applications -->
    <component name="PopRestStore" group="moqui" branch="master"/>

    <!-- Third Party Components -->
    <component name="Sales" group="xolvegroup" version="" branch="main"/>
    <component name="WorkManagement" group="xolvegroup" version="" branch="main"/>

    <!-- Component Sets -->
    <component-set name="thebizapi" components="thebizapi"/>

    <!-- Release builds:
        gradle getComponentSet -PcomponentSet=custom -PlocationType=release
        - these make the source distro for each
        - to build the demo war: gradle load test addRuntime
    -->
    <component group="thebizapi" name="thebizapi" branch="thebizapi"/>
    <component group="acetousk" name="public-suffixes" branch="master"/>
    <component group="coarchy" name="stripe" branch="master"/>
</addons>